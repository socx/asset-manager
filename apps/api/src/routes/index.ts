import { Router } from 'express';

export const router = Router();

// Auth routes — to be added in ITER-1-005 through ITER-1-012
// router.use('/auth', authRouter);

// Admin routes — to be added in ITER-1-014 through ITER-1-017
// router.use('/admin', adminRouter);
